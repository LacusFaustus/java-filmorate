# Filmorate

Сервис для оценки и рекомендаций фильмов.

## Функциональность

- Управление пользователями (создание, обновление, получение)
- Управление фильмами (создание, обновление, получение)
- Система друзей между пользователями
- Система лайков для фильмов
- Получение популярных фильмов по количеству лайков
- Поиск общих друзей между пользователями

## Технологии

- Java 17
- Spring Boot 3.2.4
- Spring Web
- Spring Validation
- Lombok
- Logbook для логирования HTTP запросов/ответов
- Maven

## Запуск приложения

1. Клонируйте репозиторий
2. Убедитесь, что установлены Java 17 и Maven
3. Выполните команду: `mvn spring-boot:run`
4. Приложение будет доступно по адресу: http://localhost:8080

## API Endpoints

### Пользователи

- `GET /users` - получить всех пользователей
- `GET /users/{id}` - получить пользователя по ID
- `POST /users` - создать пользователя
- `PUT /users` - обновить пользователя
- `PUT /users/{id}/friends/{friendId}` - добавить в друзья
- `DELETE /users/{id}/friends/{friendId}` - удалить из друзей
- `GET /users/{id}/friends` - получить друзей пользователя
- `GET /users/{id}/friends/common/{otherId}` - получить общих друзей

### Фильмы

- `GET /films` - получить все фильмы
- `GET /films/{id}` - получить фильм по ID
- `POST /films` - создать фильм
- `PUT /films` - обновить фильм
- `PUT /films/{id}/like/{userId}` - поставить лайк фильму
- `DELETE /films/{id}/like/{userId}` - удалить лайк у фильма
- `GET /films/popular?count={count}` - получить популярные фильмы

## Тестирование

Для запуска тестов выполните команду: `mvn test`

Для проверки стиля кода: `mvn checkstyle:check`
